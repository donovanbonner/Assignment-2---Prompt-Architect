{
  "name": "prompt-architect",
  "private": true,
  "version": "1.0.0",
  "description": "A middleware-style client app that sanitizes, augments, and controls LLM outputs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
